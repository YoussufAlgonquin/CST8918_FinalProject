# Remix Weather Application

TODO(owner): copy in the Week 3 Remix Weather Application source
(the same app used since `rlmckenney/cst8918-w25-a01-weather`), including
its `Dockerfile`, so this folder is a working Remix app rather than a
placeholder.

Once the app code lands here, the workflow files in
`.github/workflows/app-*.yml` (currently stubs) can be completed to build
the Docker image and deploy it to AKS.
